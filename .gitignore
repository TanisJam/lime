node_modules/
dist/

# FluidSynth playback assets — large & regenerable via
# `node apps/demo/scripts/setup-fluidsynth.mjs`.
apps/demo/public/js-synth/
apps/demo/public/soundfonts/
*.tsbuildinfo
.DS_Store
coverage/
.vite/
apps/demo/dist/
*.local
.env

# Corpus data: downloaded datasets are NEVER committed (licensing).
# Only derived StylePacks (in packages/corpus/generated/) belong in the repo.
packages/corpus/data/

# The local MIDI collection (source material, hundreds of MB) — never committed.
/midi/

# Large intermediate: the full per-file/per-section label manifest is a build
# artifact (regenerate with `pnpm corpus:label`). Kept out of generated/ (the
# demo's StylePack glob) — only the derived StylePacks belong in the repo.
packages/corpus/labels/

# Local tooling metadata (not part of the project)
.atl/

# Judge pipeline output (rendered clips + reports)
tools/judge/out/
