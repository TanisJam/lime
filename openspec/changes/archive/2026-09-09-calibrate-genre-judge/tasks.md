# Implementation Tasks: Auditable Genre-Judge Calibration

Deliver this as a three-PR chain stacked directly to main. Do not acquire, download, or commit media; do not modify production composition, renderer, model mappings/weights, or fusion arithmetic.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 1,100–1,700 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 registry + pure corpus contract; PR 2 local builder + Essentia boundary; PR 3 matrix reporting + documentation |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: Yes — resolved by maintainer acceptance of `size:exception` for the cohesive PR-1 slice.
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

Size exception: accepted for PR-1 after native accounting measured 805 changed lines against the original 400-line bound; preserve the complete validation/test contract rather than deleting coverage.

## PR 1 — Audited source of truth and pure corpus contract

**Start:** no registry record is accepted without independently reviewable source/licence/instrumental evidence. **Finish:** the checked-in registry validates deterministically and pure planning produces the compatible manifest. **Rollback:** remove only `calibration-registry.json`, `calibration-corpus.mjs`, and their focused tests; no local assets or runtime outputs are tracked.

- [x] Curate and independently audit 48 real instrumental controls from a traceable source before coding the registry; for this internal judge-training corpus, non-CC0 dataset licences are acceptable when obligations and underlying-rights caveats are recorded: record eight distinct, auditable items for each exact label `Funk/R&B`, `Jazz`, `Blues`, `Rock`, `Pop`, and `Electronic`; for every `tools/judge/calibration-registry.json` record verify a stable ID, POSIX-relative local MIDI path, source URL or stable collection ID, licence ID plus evidence URL, non-empty label rationale, explicit `instrumental` review evidence, and diversity/selection notes; reject unavailable, vocal, unknown-status, weakly licensed, or invented-placeholder candidates, and stop the change if any class (especially Funk/R&B) cannot meet this bar. <!-- sdd-owner: implementation -->
- [x] RED — add `tools/judge/test/calibration-corpus.test.mjs` fixtures and assertions for the exact ordered six-candidate array, a valid 48-item audited-shape fixture, 47/49 totals, seven/nine class balance, all required review/provenance omissions, duplicate IDs and normalized paths, out-of-set labels, vocal/unknown statuses, absolute/traversal paths, and unsupported asset extensions; verify the tests fail against the absent pure module. <!-- sdd-owner: implementation -->
- [x] GREEN — add `tools/judge/calibration-corpus.mjs` as a dependency-free ESM library with typed deterministic aggregate diagnostics and `validateRegistry(registry)`; enforce schema v1, corpus ID, fixed candidate content/order/uniqueness, exactly 48 entries/eight per class, required fields, stable unique IDs, normalized relative non-escaping local paths, and only explicit instrumental status; verify the RED validation suite passes without filesystem, process, clock, random, or subprocess access. <!-- sdd-owner: implementation -->
- [x] GREEN — implement pure `resolveAssets(registry, assetRoot, exists)` and `planCorpus(registry, { seconds, sampleRate, outputDir })` in `tools/judge/calibration-corpus.mjs`; inject existence checks, reject root escapes/missing prerequisites, order jobs by declared candidate then lexical ID, derive collision-safe `${id}.wav` files, and emit the legacy manifest fields plus additive provenance with no `generatedAt`; verify focused tests assert exact ordering, truth/candidate equality, and stable byte-equivalent pure outputs. <!-- sdd-owner: implementation -->
- [x] GREEN — commit the completed, metadata-only `tools/judge/calibration-registry.json` from the approved audit without media bytes, media checksums, or executable download instructions; verify `validateRegistry` succeeds on that exact file and an audit review can trace all 48 records to their source and licence evidence. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE/REFACTOR — add adversarial validation and planning cases to `tools/judge/test/calibration-corpus.test.mjs` for path normalization collisions, candidate-order drift, source-vs-collection-ID alternatives, and deterministic multi-error diagnostic order; simplify `calibration-corpus.mjs` only after these cases pass, then run `node --test tools/judge/test/calibration-corpus.test.mjs`. <!-- sdd-owner: implementation -->

## PR 2 — Local-only build adapter and Essentia write guard

**Start:** PR 1 is merged/rebased and its pure tests pass. **Finish:** a local user can build atomically from acquired assets, while only the exact calibration manifest can establish an Essentia normalizer. **Rollback:** remove the builder and narrow Essentia eligibility helper/guard; delete ignored `tools/judge/out/calibration/` output separately.

- [x] RED — extend `tools/judge/test/calibration-corpus.test.mjs` (or add a focused adapter test file under `tools/judge/test/`) with injected filesystem/command seams proving invalid registry, missing asset root/file, and missing SoundFont fail before directory creation, FluidSynth, or ffmpeg; prove render/transcode failure leaves neither a publishable manifest nor temporary WAV files. <!-- sdd-owner: implementation -->
- [x] GREEN — add `tools/judge/build-calibration-corpus.mjs` with `--assets-root`, optional `--registry`, `--out`, and `--seconds`; read the JSON registry, run pure validation and all local prerequisite checks before output/spawn, reject non-MIDI input, render through FluidSynth then ffmpeg into a staged ignored directory, verify nontrivial WAVs, clean temporary files on every exit, atomically publish only a complete corpus, add `generatedAt` only at this CLI boundary, and return non-zero on failure; verify mocked success emits `manifest.json` with 48 clips and mocked failures invoke no forbidden command/output path. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE/REFACTOR — cover command quoting/argument construction, one mid-batch renderer failure, transcode failure, cleanup after exception, and staged-directory rename behavior through the adapter seams; refactor only to retain a small side-effect boundary, then run the complete Node judge-tool test command. <!-- sdd-owner: implementation -->
- [x] RED — add `tools/ear/test/test_tag_essentia_calibration.py` (or the repository’s accepted adjacent lightweight Python-test target) for manifest-eligibility decisions: only `calibration === true`, `reference === true`, `task === "genre"`, the exact ordered six candidates, and eight truths per candidate may write a normalizer; saved calibration may apply to an unbalanced batch; unbalanced or non-calibration input cannot create or replace one. <!-- sdd-owner: implementation -->
- [x] GREEN/TRIANGULATE — extract or add a narrow manifest-eligibility helper in `tools/ear/tag-essentia.py` and use it solely around `essentia-calibration*.json` creation; preserve `CANDIDATE_RULES`, `GENRE_SETS`, score aggregation, model behavior, and fusion weights, then verify the Python guard tests and a static diff show those prohibited mappings/weights unchanged. <!-- sdd-owner: implementation -->

## PR 3 — Deterministic class gates, compatibility, and reviewer workflow

**Start:** PRs 1–2 provide a valid manifest and guarded normalizer behavior. **Finish:** scored calibration reports expose class-level trust gates without breaking legacy report consumption. **Rollback:** revert matrix/reporting/docs and retain the existing legacy heuristic behavior.

- [x] RED — add matrix/scorer cases in `tools/judge/test/calibration-corpus.test.mjs` for declared-order score ties, inclusive pass boundaries (4/8 top-1, 6/8 top-2, mean rank 2.5), each individual gate failure, overall success with failed Funk/R&B, known ranks/counts, tied and absent primary confusions, missing/unknown truth, inconsistent candidate sets, and missing/non-numeric scores; also fixture-test a prose-only legacy report retaining chance and exact-binomial output. <!-- sdd-owner: implementation -->
- [x] GREEN — implement pure `rankScores(scores, candidates)` and `summarizeCalibration(results, candidates)` in `tools/judge/calibration-corpus.mjs`; require numeric scores for calibration gates, rank ties by declared candidate order, calculate overall metrics/exact-binomial-compatible inputs and per-class top-1/top-2/mean true-label rank/confusion frequencies, and set `trustedForTuning` only when every inclusive threshold passes; verify all scorer RED cases pass. <!-- sdd-owner: implementation -->
- [x] GREEN — refactor `tools/judge/matrix.mjs` into a small report-file CLI over the pure scorer while preserving existing prose and scored-array output; validate shared score keys against manifest candidates when available, reject incomplete scored calibration data instead of silently subsetting it, print the deterministic class table before retained row/overall output, make failed classes explicitly `UNTRUSTED FOR TUNING`, and add `--json` stable summaries containing schema version, report path, corpus metadata, candidates, overall metrics, and ordered class results; verify legacy fixtures preserve chance/binomial behavior and calibration fixtures produce the expected terminal/JSON evidence. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE/REFACTOR — run `node --test tools/judge/test/calibration-corpus.test.mjs` and `pnpm test`; inspect `tools/judge/matrix.mjs` output for a passing boundary class and a failed Funk/R&B class to confirm no aggregate verdict claims the failed class is usable. <!-- sdd-owner: implementation -->
- [x] Update `tools/judge/README.md` with a review-first local calibration workflow covering the no-fetch/no-media-commit rule, all registry audit fields, expected local root layout, validation/build prerequisites and command, both Essentia backends plus MuQ/fusion/matrix commands, JSON evidence, class-gate interpretation, saved-normalizer reuse for unbalanced LIME sweeps, and the distinction from LIME selection and prompt-bias controls; update only wording/docblock in `tools/judge/reference-genre.mjs` to label it a retained non-authoritative filename/artist heuristic that cannot establish calibration trust; verify every documented command/path matches the new CLI and no classification behavior changes in the legacy builder. <!-- sdd-owner: implementation -->
- [x] Execute the documented full build → MuQ → Essentia (both backends) → fuse → matrix workflow only when reviewed local assets, SoundFont, binaries, and model environments exist; retain generated ignored manifest/report/JSON paths as local audit evidence, report unavailable prerequisites rather than fabricating a run, and verify no media or generated calibration output is staged for commit. <!-- sdd-owner: implementation -->
