# LIME audio judge — Qwen2-Audio-7B-Instruct (local, GPU).
# Install via:  ~/scripts/setup-lime-judge.sh
# This installs into /data/ai/judge/venv (uv, Python 3.12), not a repo-local venv —
# see the machine's CUDA-in-venv convention.
torch
transformers>=5.0
accelerate
librosa
soundfile
